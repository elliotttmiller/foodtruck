# LOCAL Vite development only: copy to command-center/.env.local.
# Values prefixed VITE_ are embedded in browser code. Public values ONLY.
# The production Pages build uses public/live-orders-config.js instead;
# that runtime config takes precedence over these VITE_* fallbacks.
VITE_SUPABASE_URL=https://xgyqjnfdqekwlofhtpcj.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_g_GB-l4C9JE-_maBbtW3MQ_PPcp040w
# VITE_SUPABASE_ANON_KEY is a supported legacy fallback if your project has
# not issued a publishable key. Prefer VITE_SUPABASE_PUBLISHABLE_KEY above.
# Square credentials and webhook keys belong in Supabase Edge Function Secrets,
# never in Vite env files. See the root .env.example for the full setup list.
